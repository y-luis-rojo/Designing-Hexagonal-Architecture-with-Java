quarkus:
  datasource:
    username: ${QUARKUS_DATASOURCE_USERNAME:user}
    password: ${QUARKUS_DATASOURCE_PASSWORD:password}
    reactive:
      url: ${QUARKUS_DATASOURCE_REACTIVE_URL:mysql://db:3306/db}
